services:
  bot:
    build: .
    container_name: telegram-ai-bot
    restart: unless-stopped
    dns:
      - 223.5.5.5
      - 8.8.8.8
    env_file:
      - .env
    volumes:
      - bot-data:/data
    environment:
      - DB_PATH=/data/bot.db

volumes:
  bot-data:
